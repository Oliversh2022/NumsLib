.safe-area-top {
  padding-top: var(--status-bar-height, 65px); /* 默认 44px 防止第一次没取到值 */
}
/* common.wxss */
.btn-primary {
  background-color: #FFD600;
  color: #000;
  border-radius: 8px;
  padding: 10px;
}
